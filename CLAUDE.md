# WhatToEat2 后端 AI 辅助开发约束规范

## 1. 项目概览与核心业务

本项目是 `WhatToEat2` 的后端服务，主要与 Android、iOS 客户端交互。

当前核心业务方向包括：

- 提供包含评分、图片、标签、位置及点评信息的菜品数据
- 接收和管理用户上传的菜品
- 接收、查询和管理用户对菜品的评分与评论
- 为客户端的菜品浏览、搜索和点评功能提供接口

项目目前处于初始开发阶段。未明确的数据模型、接口路径、审核流程、权限规则和部署方式不得自行假定；实现相关功能前必须先向用户确认。

## 2. 技术与项目结构

当前项目使用：

- Java 21
- Spring Boot
- Spring Web MVC
- Jakarta Bean Validation
- Maven Wrapper

现有代码位于：

```text
src/
├── main/
│   ├── java/com/rubp/whattoeat/server/  # 应用与业务代码
│   └── resources/                       # 配置及资源文件
└── test/
    └── java/com/rubp/whattoeat/server/  # 自动化测试
```

新增代码必须放在项目根包 `com.rubp.whattoeat.server` 下。后续需要建立分层结构时，应根据实际业务划分，并在实施前说明将新增或调整的包。

## 3. 架构与职责边界

- Controller 只负责接收请求、校验参数和返回响应，不堆积业务逻辑。
- 业务规则应放在 Service 层；数据访问应由 Repository 或同等职责的组件负责。
- 对外请求和响应优先使用独立 DTO，不直接将持久化对象作为 API 模型暴露。
- 不允许为尚未确定的需求提前设计复杂抽象、通用框架或无实际用途的扩展点。
- 新增分层、公共组件或基础设施前，必须确认当前项目确实需要。
- 修改现有架构时，应优先保持职责清晰、依赖方向单一，避免循环依赖。

## 4. API 与数据处理规范

- API 应遵循一致的 REST 风格，资源命名、HTTP 方法和状态码不得在不同接口间混用。
- 所有来自客户端的不可信输入都必须进行必要校验，优先使用 Jakarta Bean Validation。
- 请求参数、分页规则、排序方式、响应结构和错误格式未明确时，必须先追问，不可自行捏造。
- 评论、评分、图片地址、菜品名称和标签等用户输入必须考虑空值、长度、格式及非法内容。
- 不得在日志或接口响应中暴露密码、令牌、数据库凭据、内部异常堆栈等敏感信息。
- 涉及批量修改、删除、审核状态变化等操作时，必须明确操作条件与失败行为。
- 尚未确定数据库、对象存储、缓存和鉴权方案；未经用户确认，不得擅自引入或指定具体实现。

## 5. 配置与敏感信息

- 环境相关配置应放入配置文件、环境变量或受控的外部配置中，不得硬编码在 Java 代码里。
- 密码、密钥、访问令牌、数据库连接凭据等敏感信息不得提交到仓库。
- 示例配置只能使用明显的占位值，不得复制真实凭据。
- 新增配置项时，必须说明配置用途、默认行为以及是否为必填项。
- 修改现有配置文件前，应先确认是否会影响本地开发、测试或部署环境。

## 6. 测试与验证

- 新增业务逻辑时，应同时补充相应测试；修复缺陷时，应尽量增加能够复现该缺陷的测试。
- Controller、Service、Repository 应按实际职责选择单元测试或集成测试，不为追求覆盖率编写无意义测试。
- 测试名称应清楚表达场景与预期结果，并覆盖主要成功路径和关键失败路径。
- 完成修改后，至少运行与变更相关的测试；影响范围较大时运行完整测试：

```bash
./mvnw test
```

- 需要验证完整构建时运行：

```bash
./mvnw clean verify
```

- 如果测试或构建未执行、失败或受环境限制，必须如实说明，不得宣称已经验证通过。

## 7. 依赖引入要求

- 引入任何新依赖前，必须明确告知用户依赖名称、用途和影响。
- 优先使用 Spring Boot 已管理的依赖版本，避免无必要地手动覆盖版本。
- 不得为了简单功能引入体积较大或职责重叠的依赖。
- 对不确定或更新较快的库及 API，必须主动查询官方文档，不依靠过时记忆推测用法。
- 未经用户确认，不得擅自引入数据库、ORM、认证、缓存、消息队列、对象存储或 API 文档框架。

## 8. 编码与命名规范

- 包名、变量名、函数名和类名必须与仓库现有 Java 风格保持一致，不混用其他语言或框架的命名习惯。
- 类名使用 `UpperCamelCase`，方法和变量使用 `lowerCamelCase`，常量使用 `UPPER_SNAKE_CASE`。
- 命名应直接表达业务含义，避免无意义缩写以及 `data`、`info`、`manager` 等过度宽泛的名称。
- 保持代码简单清晰，不编写重复逻辑、无用注释或与实现不一致的文档。
- 使用项目现有格式；除非用户明确要求，不对无关文件进行批量格式化。
- 注释用于解释业务规则、设计原因或特殊边界，不重复描述代码表面行为。

## 9. 指令与文件操作规范

禁止执行以下破坏性指令：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

同样禁止使用 Python、Java 或其他脚本绕过上述限制进行递归删除。

- 禁止对任何系统根目录、磁盘根目录或系统级目录执行删除、清理或修改操作。
- 未经用户明确授权，禁止执行跨出当前项目根目录的写入、移动、覆盖或删除操作。
- 删除、覆盖或迁移项目内已有文件前，必须先检查内容；若与任务描述不符，应先向用户说明。
- 不得修改构建产物、IDE 缓存或依赖缓存来代替修改源代码。
- 对不可逆操作、数据库数据变更及对外发布操作，执行前必须获得明确授权。

## 10. 文件规范

- 单个源文件不宜过长，原则上尽量控制在 250 行以内。
- 文件接近或超过限制时，应按职责拆分，不能仅为减少行数进行无意义拆分。
- 一个文件应聚焦单一职责，避免将 Controller、Service、DTO 和数据模型堆放在同一文件中。
- 新增文件时应放入职责匹配的包，禁止创建含义模糊的 `utils` 或 `common` 大杂烩目录。

## 11. 新功能计划规范

- 提出修改或新增功能时，若业务规则、接口行为、数据结构或异常处理存在歧义，必须先追问。
- 不可自行虚构需求、默认值、字段、接口、角色权限或审核逻辑。
- 所有影响实现方式的细节确认完成并获得接受后，才可开始修改代码。
- 开始实现前，应简要说明预计修改或新增的文件、实现步骤、依赖变化和验证方式。
- 若实现过程中发现原方案需要扩大范围，应暂停并说明原因，不得静默扩大修改范围。

## 12. 代码修改规范

- 修改或新增代码时，只改动完成当前任务所必需的文件。
- 如需修改用户未点名的其他文件，必须提前强调文件名称、修改原因和影响。
- 非必要不进行重构、改名、依赖升级、目录迁移或配置调整。
- 必须先阅读相关代码并遵循现有实现风格，不得用整套新写法替换已有模式。
- 不得覆盖或回退用户已经存在且与当前任务无关的修改。
- 完成后应明确列出实际修改的文件、主要变化、依赖变化和测试结果。
